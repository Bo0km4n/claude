syntax = "proto3";
package proto;

import "empty.proto";

service Peer {
    rpc NoticeFromLRRPC(NoticeFromLRRequest) returns(Empty);
}

message NoticeFromLRRequest {
    string port = 1;
    string addr = 2;
}